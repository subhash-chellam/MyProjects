function fn() {
 
  var config = {
		GExecutionId: "LaerdalAPITest",
		testEndPoint: 'https://preprod-rqi1stop.laerdalblr.in/v1/api/',
		AuthURL: 'https://preprod-rqi1stop.laerdalblr.in/v1/api/tokens'
  }

  return config;
}